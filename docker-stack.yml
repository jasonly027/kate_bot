services:
  app:
    image: jasonly027/kate_bot:0.2.0
    environment:
      - RUST_LOG=info,serenity=warn
      - ENV=prod
      - DISCORD_TOKEN_FILE=/run/secrets/kate_bot_discord_token
    secrets:
      - kate_bot_discord_token

secrets:
  kate_bot_discord_token:
    external: true
